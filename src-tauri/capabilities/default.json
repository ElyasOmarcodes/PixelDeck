{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Bare window hosting. PixelDeck calls no Tauri APIs, so nothing beyond the core defaults is granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
